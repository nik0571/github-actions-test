name: deploy

on:
  workflow_call:
    inputs:
      SERVER_HOST_QA:
        required: true
        type: string

jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4.1.1
        run: echo "deplyment"