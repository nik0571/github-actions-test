name: deploy

on:
  workflow_call:
    inputs:
      SERVER_HOST_QA:
        required: true
        type: string

jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
      - name: echo result
        id: generate_rollback_folder_name
        run: echo "resulkt ${{ inputs.SERVER_HOST_QA }}"